---
description: Start the workflow for adding a new ballerina/<name> stdlib package or filling gaps against jBallerina.
---

Invoke the `add-stdlib-support` skill and walk the user through it step by step, starting with step 1 (asking for the path to the jBallerina module source). Do not skip the planning gates in steps 4 and 5.
