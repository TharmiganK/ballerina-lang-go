github_checks:
  annotations: true

ignore:
  - "test_util/**"

coverage:
  ignore:
    - "test_util/**"
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: 80%
