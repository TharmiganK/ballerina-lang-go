# Raise the WebFlux in-memory codec cap (default 256KB) above the largest
# benchmark payload (1MB) so request/response bodies buffer without erroring.
spring.codec.max-in-memory-size=16MB
# Keep startup output minimal; readiness is detected via the port.
spring.main.banner-mode=off
logging.level.root=warn
